# how to setup automatically when I open Ucloud
!/usr/bin/env

git config --global user.email "laura.soerine@icloud.com"
git config --global user.name "soerinev"

# before adding, committing and pushing to github, press "bash setup.sh", that's the same as writing the above?? not necessary because of !/usr/bin/env?? 